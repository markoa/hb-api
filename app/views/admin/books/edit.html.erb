<div class="page-header">
  <h1>Editing <%= link_to "book #{@book.id}", admin_book_path(@book) %></h1>
</div>

<%= form_for [:admin, @book], :html => { :class => "form-horizontal", :role => "form" } do |f| -%>
  <%= render "form", { :f => f } %>
<% end -%>
